[tox]
envlist = py3
isolated_build = True

[testenv]
deps =
    pytest
    pytest-codeblocks
    pytest-cov
    pytest-randomly
    mpmath
    scipy
    meshzoo
commands =
    pytest {posargs} --codeblocks
