[tox]
envlist = py3
isolated_build = True

[testenv]
deps =
    exdown
    pytest
    pytest-cov
    mpmath
    scipy
    meshzoo
commands =
    pytest --cov {envsitepackagesdir}/cplot --cov-report xml --cov-report term
